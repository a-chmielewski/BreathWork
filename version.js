/** App release version — keep in sync with CACHE_NAME in sw.js */
var APP_VERSION = '1.6.1';

if (typeof module !== 'undefined' && module.exports) {
  module.exports = { APP_VERSION: APP_VERSION };
}
